# 1.3.9 翻译服务配置整理

- 移除 DeepL 的服务类型、Free/Pro 套餐配置及请求适配。机器翻译服务保留百度、Google Cloud Translation v2 和 Microsoft Translator。
- 百度、Google、Microsoft 的配置卡片只显示服务名称，不再附加模型名或“未配置模型”；LLM 服务保持原模型信息展示。
- 读取旧设置时移除 DeepL 配置，保留其他服务及凭据。默认服务和功能绑定按现有有效服务规则恢复；仅配置 DeepL 时恢复无密钥的默认配置，不复用 DeepL 地址、请求头或密钥。清理对应会话密钥并持久化迁移结果。
- 导入含 DeepL 的旧配置文件时提示先删除对应服务项，不覆盖现有配置或申请该服务权限。
- 本次升级包版本为 1.3.9，不修改已创建的 Git 标签。

验证：类型检查与生产构建通过；91 项单元测试、40 项浏览器测试通过，覆盖三家服务卡片和旧配置迁移。ESLint 无错误，保留 7 条存量警告。已生成 `.output/flow-translate-1.3.9-chrome.zip` 并校验版本、ZIP 完整性、图标和 Alt+Q 声明。
