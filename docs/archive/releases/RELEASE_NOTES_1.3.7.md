# 1.3.7 更新说明

- 设置 → 模型服务的卡片上新增“设为默认”，与“测试”按钮采用相同样式，点击自动保存且不会打开编辑弹窗。
- 默认模型显示“当前默认”和原有默认标记；未启用的模型不可设为默认。
- 设置页的默认模型说明及“跟随默认模型”选项同步更新，工具栏下次打开时读取新默认值。各功能单独绑定的模型保持原选择。

验证：类型检查及 3 项相关浏览器用例通过，涵盖默认模型保存与实际调用、功能绑定保留及卡片测试反馈；已检查卡片截图。生产构建与 ZIP 校验通过，包内版本 1.3.7。
