# 1.3.18 功能页面切换实际服务

- 修复弹窗选择服务仅更改默认卡片、被独立划词绑定覆盖的问题。选择框展示划词绑定并更新该绑定；提供“跟随默认服务”。
- 全文翻译区域新增同风格的服务选择框，更新全文/区域翻译绑定。提示已有任务保留原服务，恢复原文后重新翻译使用新服务。
- 长文本沿用正确的独立绑定逻辑，并与弹窗复用绑定更新函数。分功能模式下不修改其他功能；统一模式下均修改共享默认服务，保留独立绑定供以后恢复。
- 弹窗保存时读取最新设置，完成后重新加载，并监听配置变化；保存过程中临时禁用服务选择与启动翻译按钮。错误显示可重试提示。

安装包：`.output/flow-translate-1.3.18-chrome.zip`。

验证：类型检查、构建、98 项单元测试通过；ESLint 无错误（7 条存量警告）。首次全量浏览器测试 57 项通过、2 项发现弹窗开关视觉更新延迟；恢复即时反馈后，相关三个文件的 32 项测试全部复测通过。59 项浏览器用例经全量及相关复测验证。测试包含从弹窗选择服务后实际划词/全文请求、任务快照、默认回退和长文本独立切换。

安装包完整性、版本、页面入口、图标及 Alt+Q 已校验。
