# 1.4.6 产品图标统一

- 插件弹窗左上角使用产品 `128.png` 图标，并按 40 × 40 像素展示。
- 设置页左上角使用同一产品图标，并按 56 × 56 像素展示。
- 移除原有文字“译”的渐变色占位图标，增加与现有界面一致的轻量投影。

安装包：`.output/flow-translate-1.4.6-chrome.zip`。

验证结果：`npm run check` 通过（105 个单元测试、61 个端到端测试）；代码检查无错误，保留 6 条既有 React Hooks 警告。
