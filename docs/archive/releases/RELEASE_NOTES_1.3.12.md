# 1.3.12 数据处理说明显示优化

- 设置页的“数据处理说明”在用户勾选同意后自动隐藏。
- 同意状态继续保存到本机，重新打开设置后保持隐藏；已有同意记录的用户无需再次勾选。
- 首次使用或执行“删除并重置”撤销同意后，重新显示说明及同意勾选项。
- 导入配置仍使用既有的本机同意校验，不以导入文件中的同意记录替代用户确认。

安装包：`.output/flow-translate-1.3.12-chrome.zip`。

验证：类型检查、生产构建通过；8 项浏览器回归测试通过，覆盖同意后隐藏、重新打开后的持久化、撤销同意后重新显示，以及导入权限和服务配置行为。ZIP 完整性、版本和页面入口校验通过。
