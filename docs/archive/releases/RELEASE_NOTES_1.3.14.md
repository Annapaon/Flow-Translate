# 1.3.14 数据处理确认

- 设置页的数据处理说明采用与弹窗一致的“了解并同意”按钮，不再提供勾选框或折叠入口。
- 默认同意状态为 `false`，必须由用户手动确认；点击后整块说明消失，再次打开仍隐藏。
- 保留此前在本机手动确认的记录；删除并重置后重新显示说明，导入文件不能替代首次手动确认。

安装包：`.output/flow-translate-1.3.14-chrome.zip`。

验证：类型检查、生产构建及 9 项浏览器回归测试通过，覆盖手动确认前后的状态、持久化、重置同意后的显示和导入同意校验。ZIP 完整性、版本及页面入口校验通过。
