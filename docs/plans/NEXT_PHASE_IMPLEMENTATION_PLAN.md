# 后续功能实现路径与技术方案

> 更新日期：2026-09-14\
> 代码基线：1.3.8（`package.json`），覆盖 1.3.0–1.3.8 已完成的功能开发。\
> 状态：核心功能已实现并形成构建产物；自动化验证与真实环境验收分别记录，不代表已发布 Chrome 商店或已完成真实服务联调。\
> 目标：保持“选择文字或网页 → 获得译文 → 继续阅读”的操作逻辑，设置按需展开。

本文已由最初的设计草案更新为当前实现说明和后续验收清单。当前行为以本文及代码为准；历史版本的设计变化和当时验证结果见各版本交付记录。一期范围仍以 [一期核对报告](../reviews/PHASE1_REVIEW.md) 为准，不因后续功能追加而改写。

## 1. 功能范围与完成状态

| 功能 | 当前入口与行为 | 状态 |
| --- | --- | --- |
| 双向互译 | 设置页及工具栏共享语言配置；开启后在原源/目标语言位置显示两种可自选、不可相同的互译语言 | 已实现 |
| 智能输出 | 设置页可选，默认关闭；按文本长度、分词和句子边界选择词汇、解释或纯翻译模式 | 已实现，本地启发式判定 |
| 重新翻译 | 划词浮窗、长文本页；绕过缓存读取，按当前有效配置重新请求 | 已实现；划词旁的服务下拉框已移除 |
| 网站暂停/禁用 | 工具栏提供会话暂停、永久禁用及解除 | 已实现 |
| 自定义术语 | 设置页折叠区域，最多 100 条有方向的术语或保留原文规则 | 已实现 LLM Prompt 路径 |
| 网页全文翻译 | 工具栏和设置页均有总开关；默认按键翻译，可选择自动翻译 | 已实现 |
| 原文下方双语排版 | 保留原节点，以浅色圆角译文框区分原文，尽量保持块级及常见行内格式 | 已实现，复杂布局可能跳过或降级 |
| 第三方翻译服务 | 现有模型服务管理中接入百度、Microsoft Translator、Google Cloud Translation v2、DeepL API | 协议适配与模拟测试完成，真实账户联调待验证 |
| 翻译性能 | 模型并发设置、交互优先、429 退避、短段落合批、视口优先、流式预览 | 已实现 |
| 按功能绑定模型 | 仅设置页“模型服务”提供开关；分别配置划词、全文、长文本模型 | 已实现，默认关闭 |
| 设置默认模型 | 工具栏模型选择，以及设置页模型卡片“设为默认” | 已实现 |
| 导入并启用 | 选择文件后统一申请接口权限并保存，不需要逐张模型卡片再次授权 | 已实现；保留 Chrome 必要权限提示 |
| 测试及错误反馈 | 模型测试反馈在对应卡片内；设置通知约 4 秒消失，切换菜单清除 | 已实现 |

界面继续使用 React、原生 CSS 和现有紫色滑动开关、圆角按钮与卡片，不引入新的大型状态或样式框架。模型分配不增加到工具栏、划词浮窗或全文控制面板中。PDF、OCR、字幕、云端账号、聊天、背词及按网站记忆语言对不属于当前交付范围。

## 2. 实际模块与调用路径

| 模块 | 当前职责 |
| --- | --- |
| `src/entrypoints/content.tsx` | 选区读取、划词浮窗、重新翻译、方向切换、流式结果、取消；启动全文控制器 |
| `src/entrypoints/route-change.content.ts` | SPA 路由通知 |
| `src/entrypoints/background.ts` | 接收并检查扩展消息来源，连接 Port、网站控制、快捷键、测试和统计接口 |
| `src/core/translation/port.ts` | 统一翻译流水线、页面会话快照、权限检查、缓存、重试和持久化 |
| `src/core/translation/model-routing.ts` | 按功能解析完整模型配置；全文快照的配置变更比较 |
| `src/core/translation/language.ts` | 语言规范化、双向判定、智能输出、定向术语匹配 |
| `src/core/translation/scheduler.ts` | 同一模型配置共享并发、交互优先、限流冷却及恢复 |
| `src/core/translation/page-batch.ts` | 同一页面内短段合批、JSON 行校验、格式错误回退 |
| `src/core/translation/meter.ts` | 按实际网络尝试计数 |
| `src/core/translation/commands.ts` | 将浏览器命令转交到划词或全文入口 |
| `src/core/providers/` | 大模型协议、预设、Prompt、SSE 和连接测试 |
| `src/core/services/machine.ts`、`capabilities.ts` | 四家机器翻译请求、认证、语言代码、响应校验和能力声明 |
| `src/content/page-translation/controller.ts`、`dom.ts` | 全文任务、DOM 分组、序列化、安全渲染、预览、动态更新和恢复 |
| `src/shared/types.ts`、`settings.ts` | 设置模型、默认值、兼容归一化和公有设置 |
| `src/shared/credentials.ts`、`security.ts`、`api-permissions.ts` | local/session 密钥、公有数据隔离、导入校验、URL 校验和接口权限申请 |
| `src/shared/history.ts`、`db.ts` | IndexedDB 历史、收藏、缓存和按模型统计 |
| options / popup / sidepanel | 设置管理、工具栏常用操作、长文本翻译 |

请求路径为：来源及访问规则检查 → 读取设置 → 确定功能与模型 → 解析方向和输出模式 → 有效缓存查询 → 接口权限检查 → 调度/合批 → 服务响应 → 渲染及持久化。

大模型继续使用 Provider 抽象；机器翻译在统一请求层分流，不经过大模型的模型名及 Prompt 校验。最初草案中的 `resolve-language.ts`、独立服务列表和独立 collector/renderer 文件并未照名创建，实际职责由上表模块承担。

## 3. 语言方向与轻量翻译行为

### 3.1 双向互译

- `bidirectional` 默认关闭；`pairSourceLanguage` 与 `pairLanguage` 分别表示语言一和语言二，默认“简体中文 / 日本語”。
- 两种语言都可以修改，不能相同。开启时替换原源/目标语言选择框，关闭时恢复原设置值。
- 旧配置缺少语言一时补为简体中文，保留原互译语言；无效或重复的选择由设置归一化处理。
- 可靠识别为语言一时译为语言二；语言二译为语言一。其他语言或无法可靠确定方向时回退语言一，并在需要时提示方向不确定。
- 划词和长文本可切换本次翻译方向；本次显式目标优先于自动判定。

判定使用 `browser.i18n.detectLanguage`、假名等文字特征及 `lang` 提示；短词、全汉字日语和混合语种可能存在歧义。没有为每段另发云端语言检测请求。实际语言解析位于 `src/core/translation/language.ts`，并在生成缓存键及请求之前完成。

双向互译是方向规则，不是独立的模型用途：划词互译使用划词模型，长文本互译使用长文本模型。全文根据页面样本文本确定一次目标语言，任务内统一目标，并跳过可靠识别为目标语言的段落。

全文界面不提供独立目标语言选择框。协议仍支持显式目标参数，但普通用户入口跟随当前单向设置或互译规则。

### 3.2 智能输出和术语

`smartOutput` 默认关闭，仅在用户选择纯翻译模式时启用自动判定；显式选择解释、词汇或语法模式时保留用户选择。当前以 `Intl.Segmenter`、文本长度及句子标点作启发式判断，不宣称具有完整语义分析能力。

全文强制纯翻译并关闭思考过程。机器翻译只发送其支持的参数，不会为了解释、思考或术语转用另一个服务；原有 LLM 设置保留。

术语最多 100 条，包含源词、目标词、源/目标语言及保留标记。仅将当前文本中命中且方向匹配的条目注入 LLM Prompt；拉丁文本匹配考虑词边界。规则不自动逆向应用，也不对译文无条件替换。机器翻译原生术语库及可靠占位符术语适配未实现。

### 3.3 重新翻译和网站控制

重新翻译使用新的 `requestId` 和 `refresh: true`，取消原请求、清空旧流式结果，跳过缓存读取并在成功后更新缓存。划词浮窗已移除服务下拉菜单，使用设置中分配的模型；不再描述为可在浮窗临时切换服务。全文“重试失败段落”只重新调度失败块。

网站会话暂停保存到 `storage.session`，永久禁用使用独立 local 域名列表，两者与原黑名单/白名单共同生效，不随模型配置文件迁移。站点暂停/禁用会阻止新翻译并清理网页译文；解除禁用不会自动绕过仍有效的其他访问规则。自动全文模式下，恢复可用条件后可能按模式重新启动，不能承诺恢复站点后一律等待手动点击。

## 4. 模型管理、默认模型与功能分配

### 4.1 默认模型

`activeModelId` 表示默认模型。工具栏选择模型或在设置页卡片点击“设为默认”均自动保存；当前模型显示默认标记及“当前默认”，停用模型不能设为默认。新增并启用模型时沿用现有逻辑，可成为当前默认项。

卡片继续管理服务参数、测试、启停和使用量。“API Key 保存方式”与卡片区域留有独立间距。模型测试结果显示在发起测试的卡片内，各卡片独立计时和清除，点击“设为默认”不会打开编辑弹窗。

### 4.2 按功能分别设置模型

此配置只放在“设置 → 模型服务”的卡片上方：

| 字段 | 默认值 | 行为 |
| --- | --- | --- |
| `separateModels` | `false` | 关闭时所有功能使用默认模型，并隐藏绑定区 |
| `featureModels.selection` | 空字符串 | 划词、右键选区及选区快捷键使用的模型 |
| `featureModels.page` | 空字符串 | 网页全文翻译模型 |
| `featureModels.longText` | 空字符串 | 侧栏长文本翻译模型 |

开启后每项可选择已启用服务，空值表示“跟随默认模型”，并显示默认模型名称。关闭后保留有效绑定，再次开启恢复。删除或停用模型会清理对应绑定并回退默认项；卡片列出绑定用途和回退说明。

改变默认模型不修改显式功能绑定。`settingsForFeature()` 以请求副本同步替换服务类型、地址、密钥、模型名、温度、超时、输出预算和请求头，后续缓存、权限、统计及并发都使用实际配置。长文本入口根据扩展侧栏页面地址识别，不能通过待翻译正文指定任意接口。

长文本页已有模型选择框在单独绑定时显示实际模型并禁用修改，提示在设置中调整；跟随默认模型时保留原操作。双向互译没有第四个模型绑定项。

### 4.3 运行中任务与设置变更

全文启动时保存模型及方向快照。只修改 `separateModels` 或 `featureModels` 不会打断已有全文任务，新分配对后续任务生效。模型参数、默认项、语言配置或访问规则等其他相关配置变化仍沿用暂停保护，继续时重新读取配置，避免静默混用结果。

## 5. 第三方服务与数据结构

### 5.1 已接入服务

| 服务 | 默认地址与凭据 | 当前协议能力 |
| --- | --- | --- |
| 百度翻译 | `https://fanyi-api.baidu.com/api/trans/vip/translate`；App ID、Secret | POST form，MD5 签名；单块纯文本、自动源语言；多行响应合回当前块 |
| Microsoft Translator（必应） | `https://api.cognitive.microsofttranslator.com/translate`；Key、按资源填写 Region | JSON 文本数组，`api-version=3.0`，`textType=html/plain` |
| Google Cloud Translation Basic v2 | `https://translation.googleapis.com/language/translate/v2`；API Key | `q` 数组、语对与 HTML/text；通过 `X-Goog-Api-Key` 发送密钥 |
| DeepL API | `https://api-free.deepl.com/v2/translate` 或 `https://api.deepl.com/v2/translate`；API Key | `DeepL-Auth-Key`、文本数组及 HTML 模式，可配置自管兼容网关 |

这些是用户自行开通的开发者 API。当前没有接入免密钥的谷歌旧接口、网页抓取或内置共享密钥；Google Advanced v3、原生术语库和开发者运营的代理服务均未加入。

语言代码及厂商响应在 `src/core/services/machine.ts` 转换和校验。响应数量不匹配、空结果、认证/配额及限流错误有对应处理，错误展示前脱敏。四家服务的协议及扩展链路已使用模拟接口验证；真实账户额度、地区访问、DeepL 直连等仍需单独联调。

### 5.2 持久化兼容决策

当前继续沿用 `ModelProfile`、`modelProfiles`、`activeModelId`，不创建第二份独立服务列表。`schemaVersion` 为 2；新增 `kind`、`appId`、`region`、`maxConcurrency` 等字段，百度 Secret 使用现有 `apiKey` 通道。

`src/shared/settings.ts` 负责唯一归一化入口，并将默认模型参数同步到兼容顶层字段。模型 ID、local/session 密钥方式和原有导出格式继续兼容。机器翻译允许模型名为空，UI 隐藏不适用字段；持久化结构仍可保留这些兼容参数，不采用最初草案中的独立 `credentialRef` 或服务 discriminated union。

导入采用 Zod 字段校验及按服务补充验证。默认导出剥离 API Key 和自定义请求头，含密钥导出仍需明确确认。公有设置仅暴露界面所需的名称、规则等数据，不暴露接口凭据；内容脚本不能提供任意转发 URL。

## 6. 接口权限与导入并启用

保存、测试和导入统一使用 `src/shared/api-permissions.ts`：仅请求配置接口所需的主机权限，不申请所有网站的必需 API 访问权。插件自身的“是否继续授权”对话框已取消，Chrome 必要提示仍由浏览器显示；已有权限无需重复确认。

导入路径：

1. 选择 JSON 文件，限制文件大小并校验配置；此时不覆盖现有设置。
2. 展示文件及模型数量，用户点击“导入并启用”。
3. 在按钮事件中直接发起权限申请，前面不等待文件读取、存储或权限查询，以保留用户操作上下文。
4. 合并已启用模型的接口主机；停用模型不额外申请。如果全部模型停用，按归一化规则处理首个模型。
5. 授权成功后按保存队列写入配置；不必再逐张卡片保存或测试。拒绝时保留原配置，可直接重试。

导入文件里的隐私同意标记不可信。当前 UI 保留本机已确认的数据处理同意，首次使用可在导入区域明确确认；不再要求每次导入都撤销本机已有同意。配置启用了自动全文模式时，导入并启用后可能开始处理符合规则的页面。缺少有效密钥或接口不可用时，导入成功不等于服务调用成功。

切换设置页面、取消或重新选择文件，会废弃尚未提交的旧导入；双击不会重复启动导入，晚到的授权或文件读取结果不能覆盖新操作。

静态主机权限仅包含本机 HTTP 地址 localhost、127.0.0.1、::1；其他地址按需申请。云端地址必须 HTTPS，HTTP 仅允许符合校验的本机和私有局域网地址。权限按主机合并，不把不同路径或端口当成独立站点。授权不会自动发起模型测试或向开发者上传配置。

## 7. 全文翻译交互与页面实现

### 7.1 开关、模式和快捷键

- `pageTranslationEnabled` 默认 `true`，工具栏与设置页均可开关；关闭后取消任务并恢复原文。
- `pageTranslationMode` 默认 `manual`，界面名称为“按键翻译”；另一选项为“自动翻译”。
- 全文命令 `translate-page` 的默认声明为 `Alt+Q`，描述为纯中文“翻译当前网页全文”。Chrome 决定实际绑定；冲突或已有用户设置可能影响结果。
- 工具栏按键模式显示大号“点击翻译（实际快捷键）”，从 `browser.commands.getAll()` 读取；点击受理后关闭工具栏，网页控制器继续任务。
- 设置页只展示快捷键和修改入口，不从设置页启动不明确的网页。`chrome://extensions/shortcuts` 是实际绑定的修改位置。
- 1.3.8 已删除“默认 Alt + Q；如有冲突，请重新绑定。”提示，但保留未绑定状态和“修改快捷键”，不伪造默认键已经生效，也不添加绕过浏览器设置的网页键盘监听。

全文区域不显示独立的模型选择、目标语言选择或用量估算。任务期间保留完成/总数、失败、跳过、格式降级计数，以及暂停、继续、恢复原文和失败重试。使用量统一在模型卡片中查看。

自动模式在符合网站规则、已经同意数据处理的普通主框架网页上启动，处理 SPA 导航和晚加载正文；不自动滚动、展开折叠内容或抓取未加载的下一页。用户暂停或恢复原文后，不立即在同一页面重新自动启动。

### 7.2 发现可翻译块与对应关系

`collectGroupsAsync()` 分片遍历可读 DOM，将段落、标题、列表项、引用、表格单元格等组成独立语义块，避免父子重复收集。排除脚本、样式、表单与编辑区、隐藏文本、代码块、媒体、导航/按钮、不翻译标记及扩展宿主。

块保存随机 ID、`Group`、序列化原文、状态、宿主引用和变化次数。页面侧使用 anchor、原文结构比较、扫描版本及 `epoch` 阻止陈旧回填；实际协议并未增加独立的 `sessionId/blockId/revision` 全套字段，而是使用独立 Port 会话与 `requestId`。

源文变化只替换对应块；节点消失时释放引用。频繁变化或不支持的布局块会跳过。相同文本仍在不同位置拥有独立块身份，缓存可复用译文，但不能猜测批量返回的对应关系。

### 7.3 原文下方插入与格式

译文使用带 `data-flow-translation` 标记的 `span` 宿主，在合法锚点后以块级正常流显示，内部用 Shadow DOM 隔离。列表在当前项原文后插入，表格在单元格内插入，原 DOM 不替换或整体重建。当前对 Flex/Grid 等难以安全放置的块采取保守跳过，不承诺自动适配所有复杂卡片。

宿主复制字号、字体、字重、行高、对齐等受控排版属性，并使用浅紫底色、细边框和约 9px 圆角区分原文。主题及窗口变化会更新样式。不同语言长度不同，不承诺相同换行、高度或像素级一致。

行内结构转成最小 HTML：常见粗体、斜体、下划线、换行和 span。链接地址与代码占位映射留在本地；`parse5` 解析服务结果后检查标签和 `data-ft-id`，再通过 DOM API 重建。服务生成的新 URL、事件或任意样式不能进入页面；代码从本地映射恢复。结构错误退为安全纯文本并计入格式降级。

百度使用纯文本。LLM、Microsoft、Google、DeepL 走 HTML 能力路径；LLM 不再统一归入“只能纯文本”的分支。恢复原文只移除当前控制器持有的宿主，保留网页原节点、事件和运行状态。

### 7.4 任务状态、消息和恢复

页面任务状态实际为 `idle / starting / running / paused / completed`，块状态为 `pending / running / done / failed / skipped`。部分块失败不等于整页失败；恢复原文回到 `idle`。

独立 `page-translation` Port 通过 `page-start` 初始化快照，再发送 `translate` 和 `cancel`。服务事件使用 `start / delta / reasoning / retry / finish / error`；开始事件携带实际语言、服务名、HTML 能力及页面并发提示。划词和侧栏使用 `translation-stream`。

暂停中止未完成工作、移除预览并保留成功译文；恢复时重连并处理仍有效的未完成块。断线会暂停，不能假设 Port 能使 MV3 Service Worker 永久在线。路由变化、页面退出、恢复原文和访问规则变化有清理路径。

初始处理预算为 100,000 原文字符，达到后暂停，继续可扩充预算；这是产品处理预算，不是厂商计费额度。MutationObserver 忽略扩展宿主变化，配合版本检查及变化次数上限防止反复翻译。

## 8. 并发、合批、流式与资源保护

### 8.1 模型并发与请求优先级

“设置 → 模型服务 → 编辑卡片 → 高级设置”提供 `maxConcurrency`，默认 2，范围 1–6。前端根据模型设置准备块，后台限制实际网络请求；一个批次只占一个请求槽。

调度键包含接口 origin、凭据和模型配置 ID 的哈希。同一配置被划词、全文、长文本和连接测试使用时共享上限；不同配置独立，不宣称多个配置使用同一账户时已自动共享厂商总配额。

交互请求优先于排队中的全文请求，已发出的请求不被抢占。百度强制单并发，发起间隔至少 1.1 秒；其他服务保留 100 毫秒间隔。429（含百度 54003）触发队列冷却及并发降低，尊重 Retry-After，成功后逐步恢复。翻译重试有界；致命认证或配额错误停止排队工作。

### 8.2 短段落合批与流式预览

| 项目 | 当前实现 |
| --- | --- |
| 合批窗口 | 同一页面 Port 内等待约 20ms，不跨页面或隐私上下文 |
| 单批规模 | 最多 4 段；单段不超过 800 字符；总量受 2400 字符及 LLM 输出预算约束 |
| 合并条件 | 模型、语言、有效配置、术语/Prompt 及格式一致；长段、低输出预算等使用单段路径 |
| 机器翻译 | Microsoft、Google、DeepL 使用原生数组；百度不合并独立块 |
| 大模型 | JSON 行 `{id,text}`，校验 ID、重复、缺失、空值与大小，按 ID 返回对应段落 |
| 格式不兼容 | 清除预览，退回单段请求，本次页面会话停止新合批 |
| 部分取消 | 仅取消该段接收，其他段可继续；批内全部取消则中断共享请求 |
| 调度顺序 | 每次补充工作时视口内优先，其他块按距视口的距离排列 |
| 提前显示 | 单段流式输出约 80ms 节流预览；批量收到完整可校验 JSON 行后即可预览 |

这替代了最初“必须等待完整块才显示”的设计。预览仍经过安全渲染，重试、取消或失效时清理；仅完整成功的结果进入缓存。合批可能降低请求开销，但不承诺真实服务固定提速倍数。

长文本按内部预算拆分：机器翻译约 1800 字符、LLM 约 8000 字符，尽量在句界/空白处分段并有溢出保护；HTML 保持结构并限制块大小。这些是实现中的保守阈值，不是对全部厂商官方限额的完整元数据建模。

### 8.3 缓存、历史与用量

有效缓存键以 SHA-256 摘要隔离原文、实际模型/接口/凭据、语对、Prompt、术语及影响输出的参数。全文和非全文使用不同前缀，HTML/text 分开；不在键中明文保存密钥。划词与侧栏使用非全文缓存，全文按约 10 MiB、7 天 TTL 和 LRU 独立清理，避免挤掉原有约 100 条非全文缓存。

全文不逐段写入普通历史。历史和收藏保留原有行为；在可识别的隐身标签页上下文中不持久化历史、缓存和用量。

模型卡片分别显示成功完成数、输入/输出字符和真实 API 尝试次数。一次合批可能完成多段，但仅按实际网络请求计 API 尝试；缓存命中不计新 API 调用，已经发生的失败或取消调用仍计数。连接测试归入实际测试卡片，草稿未保存时不冒充已有卡片统计。这些是本地估算统计，不是厂商 Token 账单。

后台在异步读取后、发送前、缓存和历史写入队列中检查取消；端口断开或会话失效后拒绝迟到请求。已提交的成功数据不因随后关闭界面而回滚，不能承诺网络重试 exactly-once 或取消后服务端不计费。

## 9. 交付演进与验证记录

| 版本 | 主要交付 | 记录 |
| --- | --- | --- |
| 1.3.0 | 统一语言与服务流水线、四家机器翻译适配、全文核心及安全回填；随后补充任意语言对与样式调整 | [开发交付记录](../releases/RELEASE_NOTES_1.3.0.md) |
| 1.3.1 | 全文总开关、自动/手动模式、模型统计与译文样式 | [更新说明](../releases/RELEASE_NOTES_1.3.1.md) |
| 1.3.2 | 卡片内测试反馈、Alt+Q、大号全文按钮和模式名称 | [更新说明](../releases/RELEASE_NOTES_1.3.2.md) |
| 1.3.3 | 模型并发、合批、视口优先、退避和预览 | [更新说明](../releases/RELEASE_NOTES_1.3.3.md) |
| 1.3.4 | 删除划词“重新翻译”旁的服务下拉框 | [更新说明](../releases/RELEASE_NOTES_1.3.4.md) |
| 1.3.5 | 按功能分配模型及当前全文快照规则 | [更新说明](../releases/RELEASE_NOTES_1.3.5.md) |
| 1.3.6 | 导入并启用、合并接口授权及本机同意保留 | [更新说明](../releases/RELEASE_NOTES_1.3.6.md) |
| 1.3.7 | 模型卡片设置默认模型 | [更新说明](../releases/RELEASE_NOTES_1.3.7.md) |
| 1.3.8 | 删除快捷键冲突提示，保留真实绑定展示 | [更新说明](../releases/RELEASE_NOTES_1.3.8.md) |

原 A–D 阶段的核心规则、服务适配、全文、安全及动态页面代码已落地；E 阶段的智能输出和术语也已实现。按网站记忆语言对等可选增强不因阶段完成而视为已经开发。最初提出的原生术语库、结构化 runs、独立凭据对象、逐条重试历史关联等未采用，不应作为现有 API 使用。

### 9.1 自动测试与执行方法

| 测试文件/范围 | 关注内容 |
| --- | --- |
| `tests/unit/next-phase.test.ts`、`shared.test.ts`、`providers.test.ts` | 语言、迁移、凭据、服务协议、缓存、取消和 SSE |
| `tests/unit/page-dom.test.ts` | DOM 分组、合法插入、格式及恶意返回处理 |
| `tests/unit/performance.test.ts` | 并发、429 冷却、合批 ID、回退及取消 |
| `tests/unit/model-routing.test.ts` | 功能分配、默认回退、导入迁移及快照比较 |
| `tests/unit/api-permissions.test.ts` | 主机去重、同步申请、本机边界、拒绝及失败 |
| `tests/e2e/translation.spec.ts`、`next-phase.spec.ts` | 实际 Chromium 扩展中的选区、全文、网站控制、动态页面、样式和统计 |
| `tests/e2e/model-routing.spec.ts`、`import-permissions.spec.ts` | 实际模型路由、设置默认、绑定记忆、导入启用及授权时序 |

```bash
npm run typecheck
npm test
npm run test:e2e
npm run zip
```

最近一次完整记录为 1.3.6 的 89 项单元测试和 39 项浏览器用例通过；1.3.7 增加默认模型用例并运行 3 项相关回归，1.3.8 运行 2 项快捷键相关回归。各版本均有相应类型检查、构建和 ZIP 校验记录。不能把这些不同版本的记录表述成“1.3.8 已完整重跑全部测试”；本次文档更新也不新增测试通过结论。

自动测试使用本地模拟服务，不消耗用户真实密钥。外部接口的授权结果部分通过模拟 `permissions.request` 验证，Chrome 原生权限弹窗的人工操作与真实系统快捷键分配不在这些模拟结论之内。

### 9.2 仍需真实环境验收

- 四家官方 API 在实际账户、地区网络和语言组合下的连通性、额度、质量及错误行为；尤其是 DeepL 扩展后台直连和用户自管网关。
- Chrome/Edge 多版本、不同操作系统的 Alt+Q 分配、原生授权、旧版扩展冲突和升级行为。
- 真实中英日文章、复杂列表/表格、固定高度布局、虚拟列表及频繁更新页面的排版与恢复。
- 大型页面长时间运行、后台回收后恢复、多标签页和同账户多个模型配置的实际限额表现。
- 商店提交版本与公开隐私政策链接及内容的一致性；构建成功不等于商店已审核或已发布。

## 10. 后续范围与维护约束

待选增强包括按网站记忆语言对、机器翻译原生术语库、Google Advanced v3、更多复杂布局适配和更完整的厂商限额元数据。免费谷歌接口只有讨论与调研，当前没有实现。PDF、OCR、字幕及账号等功能保持范围外。

继续开发时应保留以下约束：

- 默认统一模型、手动全文模式和按需展开的界面，避免在每个翻译面板重复配置。
- 每个入口使用实际分配模型生成缓存、调度及统计，不能只更新页面显示而不改变后台路由。
- 不因暂停、导航、设置变化或导入异步完成而发送过期请求；明确快照和持久化边界。
- 返回内容均不可信，原 DOM 与交互状态优先，复杂排版允许明确跳过或安全降级。
- 权限只面向实际接口，同意不能从外部配置继承；导入拒绝不能覆盖已有设置。
- 小功能或修复发布时递增补丁版本并校验安装包；仅文档更新不要求改代码版本或重新打包。

相关资料：[隐私政策](../publishing/PRIVACY_POLICY.md)、[商店提交说明](../publishing/STORE_SUBMISSION.md)、[安全检查记录](../reviews/SECURITY_REVIEW_2026-09-10.md)。厂商协议参考保留为联调入口：[百度](https://api.fanyi.baidu.com/doc/23)、[Microsoft](https://learn.microsoft.com/en-us/azure/ai-services/translator/text-translation/reference/v3/translate?tabs=url)、[Google](https://docs.cloud.google.com/translate/docs/authentication)、[DeepL](https://developers.deepl.com/api-reference/translate/request-translation)、[Chrome Commands](https://developer.chrome.com/docs/extensions/reference/api/commands)。服务价格和配额应在真实联调时核对，不在本文写死。
